<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :goal_id %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name%><br>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.select :status, options_for_select([["Incomplete", "Incomplete"], ["Complete", "Complete"]]) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
